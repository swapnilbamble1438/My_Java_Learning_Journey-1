/*
 
 Introduction to Stream:
1) These streams are related to Collection Framework
 /(Group of Objects). These streams are very
  different from io stream, io streams
  are the sequence of data.
2) These streams was introduced in java 1.8 version
3) Stream API is basically bulk operations
   and process the objects of collection
4) Streams reduced the code length 
  

*/